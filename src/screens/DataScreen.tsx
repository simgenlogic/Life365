import { SurfacePlaceholder } from '../components/SurfacePlaceholder';

export function DataScreen() {
  return (
    <SurfacePlaceholder
      title="Data"
      description="Complete savefile export and validated restore will live here, with record counts and explicit confirmation before replacement."
      plannedFor="a later Packet 0001 milestone"
    />
  );
}
