import { SurfacePlaceholder } from '../components/SurfacePlaceholder';

export function LedgerScreen() {
  return (
    <SurfacePlaceholder
      title="Ledger"
      description="A chronological record of captures and unresolved opportunities, with filters, will live here."
      plannedFor="a later Packet 0001 milestone"
    />
  );
}
