import { SurfacePlaceholder } from '../components/SurfacePlaceholder';

export function GenerateScreen() {
  return (
    <SurfacePlaceholder
      title="Generate"
      description="Packet-defined output recipes will compile a structured Context Packet and copy-ready Markdown here."
      plannedFor="a later Packet 0001 milestone"
    />
  );
}
