import { SurfacePlaceholder } from '../components/SurfacePlaceholder';

export function DashboardScreen() {
  return (
    <SurfacePlaceholder
      title="Dashboard"
      description="Carry Forward, Available Now, and Catch Up sections will render here once packet installation and opportunity generation land."
      plannedFor="a later Packet 0001 milestone"
    />
  );
}
