import { SurfacePlaceholder } from '../components/SurfacePlaceholder';

export function CatchUpScreen() {
  return (
    <SurfacePlaceholder
      title="Catch Up"
      description="Unanswered opportunities still inside their recovery window will appear here for neutral, low-friction backfill."
      plannedFor="a later Packet 0001 milestone"
    />
  );
}
